---
interface Props {
  text: string;
  route: string;
  classVariant: string;
}

const { text, route, classVariant } = Astro.props;
---

<a
  href={route}
  target="_blank"
  rel="noopener noreferrer"
  class={`btn ${classVariant}`}
>
  {text}</a
>
